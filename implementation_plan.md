# 长安大学教务现代化课表与信息聚合系统 实现方案

## 1. 调研与可行性验证结果

**结论：100% 完全可行，且核心逆向与数据抓取逻辑已全部验证通过！**

针对长安大学教务系统（EAMS）与统一身份认证平台（IDS），我们已对真实网络请求与数据流完成了深度逆向工程分析，使用你的测试账号已成功实现无缝登录与全量数据解析。

---

### 1.1 认证机制逆向 (IDS 统一身份认证)
* **登录入口**：`https://ids.chd.edu.cn/authserver/login?service=http://bkjw.chd.edu.cn/eams/home.action`
* **表单标识**：`#pwdFromId`（用户名密码登录表单）
* **加密算法**：前端在提交前通过 `encrypt.js` 对密码进行 **AES-128-CBC** 加密：
  - **动态加盐 (Salt)**：页面中隐藏的 `<input id="pwdEncryptSalt">` 每次刷新动态生成（例如 `KOgqsRebJbYSkdBg`）。
  - **明文预处理**：`64位随机字符 + 明文密码`。
  - **初始向量 (IV)**：`16位随机字符`（字典 `ABCDEFGHJKMNPQRSTWXYZabcdefhijkmnprstwxyz2345678`）。
  - **填充与编码**：PKCS7 Padding，输出 Base64 字符串。
  - **状态令牌**：提取 `execution`（如 `e1s1`）。
* **验证码检测**：调用 `/authserver/checkNeedCaptcha.htl?username=...`，正常登录时为 `{"isNeed":false}`，无需图形验证码；如需验证码也可自动获取图片并呈现。

---

### 1.2 教务数据接口与数据结构解析 (EAMS)
登录成功并获得 `JSESSIONID` Cookie 后，我们已成功定位并解析了以下数据接口：

1. **课表核心数据**：
   - **请求接口**：`POST /eams/courseTableForStd!courseTable.action`
   - **请求参数**：`ignoreHead=1`, `setting.kind=std`, `semester.id=262`, `ids=193707`（用户绑定的系统学生内部ID）
   - **数据结构**：
     - 前端页面通过 JavaScript 生成 `TaskActivity` 实例：
       ```javascript
       activity = new TaskActivity(teacherIds, teacherNames, taskId, courseName, courseCode, roomId, roomName, weekMask, ...);
       index = dayOfWeek * 11 + periodSlot;
       table0.activities[index].push(activity);
       ```
     - 每一节课包含：课程名称、课程代码、主讲教师、上课地点（如 `*WX2304` 渭水校区）、上课周次位掩码（如 `011111111000...` 代表第 1~8 周生效）、星期几（0~6 对应周一至周日）和具体节次（1~11节）。
     - 同时解析下方的课程列表表格，包含学分、课程性质、开课院系等元数据。
   - **实测已解析课程样例（2026-2027学年1学期）**：
     - 《土地规划设计》 (2学分) | 任朝霞老师 | 地点：*WX2304 | 周一 3-4节、周五 3-4节 (1-8周)
     - 《土地整治施工与管理》 (2学分) | 李尚颖老师 | 地点：*WX2106# | 周二 3-4节、周四 3-4节 (1-8周)
     - 《科技论文写作》 (2学分) | 赵永华, 贾夏老师 | 地点：*WX2306 | 周二 5-6节、周四 5-6节 (9-16周)
     - 《土地整治与生态修复课程设计》 (2学分) | 韩磊等老师 | 地点：*WX2302 | 周五 1-8节 (13-16周)
     - 《形势与政策（七）》 (0.25学分) | 李婷老师 | 14-15周

2. **学期切换接口**：
   - **请求接口**：`POST /eams/dataQuery.action` (参数 `dataType=semester`)
   - **返回数据**：支持历年所有学期（从 2023-2024 一直到最新的 2026-2027 学年）。

3. **学生基础学籍档案**：
   - **请求接口**：`GET /eams/stdDetail.action`
   - **已验证学籍信息**：某某同学 | 土地工程学院 | 土地整治工程专业 | 2023级 | 渭水校区

4. **成绩与 GPA 接口**：
   - **请求接口**：`POST /eams/teach/grade/course/person!historyCourseGrade.action?projectType=`
   - **数据结构**：各学期总学分、门数、平均绩点（GPA）统计。

5. **考试安排接口**：
   - **请求接口**：`GET /eams/stdExamTable.action`
   - **数据结构**：考试科目、考试时间、考场地点、座位号等。

---

## 2. 拟构建产品形态方案对比

为了给你带来最好的使用体验，提供以下两种最佳形态方案，请选择你最心仪的方向：

| 方案 | 形式 | 优势 | 适用场景 |
| :--- | :--- | :--- | :--- |
| **方案 A (推荐)：现代化响应式 Web 应用 + 本地离线版** | 基于 Python (FastAPI/Flask) + 现代化精致 HTML5/CSS3/Vanilla JS | 1. 界面极度美观（支持毛玻璃磨砂、流光质感、暗黑模式切换）<br>2. **电脑和手机都能看**（局域网Wi-Fi下手机扫码/输入IP即可在手机浏览器全屏使用）<br>3. 支持导出 `.ics` 日历文件（一键导入苹果/华为/小米系统日历及手表）<br>4. 支持本地缓存，即使学校教务网卡顿崩溃，依然秒开查看课表 | 日常电脑端或手机端随手查看、桌面快捷方式打开、日历同步 |
| **方案 B：原生桌面客户端软件 (PyQt6 / WebView)** | 单独的 Windows `.exe` 可执行软件 | 1. 拥有原生窗口和托盘图标<br>2. 可支持“桌面半透明悬浮课表便签”挂在桌面边缘<br>3. 开机自启与上课前弹窗提醒 | 纯 PC 端深度使用，喜欢桌面常驻小组件的用户 |

> [!TIP]
> 我们可以采用 **方案 A** 作为核心，并通过一个便捷的 `启动.bat` 或封装单文件，平时不仅能在电脑浏览器秒开，还能在同一宿舍或校园 Wi-Fi 下直接用手机手机访问并添加到手机主屏幕，实现媲美 App 的体验！同时支持日历一键导出功能。

---

## 3. 功能设计与视觉风格

### 3.1 核心功能特性
1. **智能优雅登录**：
   - 界面简洁大气，记住学号密码，支持自动登录 / 记住凭证。
   - 离线缓存模式：成功抓取一次后，所有课程数据加密保存在本地；断网或教务系统维护时仍能瞬间显示。
2. **现代化课程表视图**：
   - **周课表视图**：7 天 x 11 节矩阵，卡片采用马卡龙柔和渐变色与高质感圆角，不同课程色彩分明；
   - **周次切换器**：可自由切换第 1~20 周，高亮当前教学周，自动过滤非本周课程；
   - **“今日课程”看板 (Today's Hero Card)**：首屏最上方直接呈现今天还要上的课程、教室、倒计时以及下一节课位置；
   - **课程详情模态弹窗**：点击课程卡片弹出精致半透明面板，查看完整教师、周次范围、节次、学分及备注。
3. **学期与历史切换**：
   - 下拉无缝切换大一至大四各个学期课表。
4. **扩展模块**：
   - **成绩与 GPA 查看页**：卡片化展示各学期学分与绩点走向；
   - **日历同步导出 (ICS)**：一键下载 `.ics` 文件，手机一键导入日历，自动带上课提醒！

### 3.2 视觉美学标准
- **色彩设计**：优雅深邃的深色模式（Dark Slate / Midnight Navy）与通透温润的浅色模式（Alabaster Clean），搭配柔和高饱和度的课程配色；
- **排版字体**：现代非衬线字体家族（Inter / HarmonyOS Sans / PingFang SC）；
- **微交互**：课程卡片悬浮上浮微动效、周次切换平滑过渡、毛玻璃磨砂（Backdrop Filter Blur）。

---

## 4. 实施规划 (Proposed Changes)

### 后端与抓取核心
- [NEW] `chd_auth.py`：统一身份认证模块，封装 AES 动态加密、会话管理、验证码探测与自动登录；
- [NEW] `chd_eams.py`：EAMS 爬虫与数据解析器，解析课表 TaskActivity、学期列表、成绩与学籍；
- [NEW] `server.py`：轻量极速 API 服务，提供数据接口、本地缓存与 ICS 日历生成；

### 前端现代化界面
- [NEW] `static/index.html`：现代化前端单页，包含登录页、主课表面板、周切换栏、课程详情弹窗；
- [NEW] `static/css/style.css`：高颜值原生 CSS，支持暗黑/浅色双模式、毛玻璃效果、响应式适配；
- [NEW] `static/js/app.js`：前端交互逻辑，状态管理、动态周课表渲染、离线存储；

### 便捷启动脚本
- [NEW] `启动课表.bat`：Windows 一键双击运行脚本，自动打开浏览器。

---

## 5. 验证计划

1. **接口验证**：使用已测通的代码验证学号测试账号的全量登录、解析和格式化输出；
2. **前端与视觉验证**：在本地浏览器加载界面，测试各周次（如第 1 周、第 9 周、第 13 周）不同课程的显示情况与重叠冲突处理；
3. **设备适配验证**：验证 PC 宽屏视图与手机移动端窄屏视图的自适应表现；
4. **ICS 导出验证**：验证生成的 `.ics` 日历文件能否被系统日历正确解析并带上地点与时间。
